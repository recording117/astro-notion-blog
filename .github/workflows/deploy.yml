name: Deploy

on:
  schedule:
    - cron: '5 23 * * 0' # 08:05(Mon) JST
  workflow_dispatch:

jobs:
  curl:
    name: Kick deploy hook
    runs-on: ubuntu-latest
    steps:
      - name: curl
        run: curl -X POST 'https://api.cloudflare.com/client/v4/pages/webhooks/deploy_hooks/6e548db3-2ca8-4bf7-803d-85743769655e'
